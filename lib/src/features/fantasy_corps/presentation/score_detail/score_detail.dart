import 'package:flutter/material.dart';

class ScoreDetail extends StatelessWidget {
  const ScoreDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
